/**
 * Placeholder minimal en attendant le module Utilisateurs & Rôles
 * (ARCHITECTURE.md §11 : le chauffeur y sera un `Utilisateur` avec le rôle Chauffeur).
 */
export type Driver = {
  id: string;
  nom: string;
};
